# 卡牌工坊存档 JSON Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将卡牌工坊重构为可在任意状态导入、并能通过内嵌 Base64 插图完整恢复卡面与裁剪状态的版本化 JSON 存档流程。

**Architecture:** 在现有游戏卡 schema 旁增加版本化工坊存档 schema，并用纯函数负责新版文档创建、序列化和旧格式兼容解析。浏览器图片 URL 在导出边界统一转换为 Data URL；`CardForgePage` 只负责文件选择、下载和 React 状态更新，导入入口始终渲染。

**Tech Stack:** Next.js App Router、React 19、TypeScript strict、Zod v3、Vitest、浏览器 `fetch`/`Blob`/`btoa`、现有 `downloadBlob`。

---

## 文件结构

- Create: `lib/card-forge/document.ts` — 工坊存档协议的创建、序列化和兼容导入 mapper。
- Create: `lib/card-forge/image-data.ts` — 图片 Data URL 判断、Blob 转 Data URL、URL 下载转换。
- Modify: `lib/schemas/game-card.ts` — 工坊文档 schema、图片来源 schema 和协议常量。
- Modify: `components/card-forge/CardForgePage.tsx` — 始终显示存档入口，接入 Base64 导出和校验导入。
- Create: `tests/card-forge-document.test.ts` — 新版文档 round-trip、无图、兼容格式和非法输入测试。
- Create: `tests/card-forge-image-data.test.ts` — Data URL 判断、Blob 转换、远程图片成功/失败测试。
- Modify: `tests/generate-game-card-handler.test.tsx` — 未生成卡牌时导入入口可见、导出按钮禁用测试。

现有 `GameCardMetadataSchema`、`image-crop.ts` 和 `GameCardFace` 保留，新增协议复用它们的类型和裁剪默认值；不改 API、不改数据库。

## Task 1: 建立版本化工坊存档协议

**Files:**

- Test: `tests/card-forge-document.test.ts`
- Modify: `lib/schemas/game-card.ts`
- Create: `lib/card-forge/document.ts`

- [ ] **Step 1: 写新版文档与兼容导入的失败测试**

在 `tests/card-forge-document.test.ts` 中使用最小但完整的 `GameCardFaceData` fixture，先锁定以下公开接口和行为：

```ts
import { describe, expect, test } from 'vitest';
import type { GameCardFaceData, ImageTransform } from '@/lib/schemas/game-card';
import {
  buildGameCardForgeDocument,
  parseGameCardForgeImport,
  serializeGameCardForgeDocument,
} from '@/lib/card-forge/document';

const faceData: GameCardFaceData = {
  cardName: '蔷薇荆棘',
  cardType: 'character',
  rarity: 'epic',
  cost: 5,
  element: 'dark',
  attack: 7,
  defense: 4,
  hp: 8,
  effects: [{ type: '被动', description: '荆棘缠绕' }],
  traits: ['控制'],
  flavorText: '盛放于黑夜。',
  powerLevel: 'S',
  description: '测试卡牌',
  themeColor: '#8b5cf6',
};

const transform: ImageTransform = { scale: 1.25, x: 0.2, y: -0.3 };
const imageDataUrl = 'data:image/png;base64,aGVsbG8=';

describe('card forge document', () => {
  test('round-trips face data, embedded image and crop state', () => {
    const json = serializeGameCardForgeDocument({
      faceData,
      imageDataUrl,
      imageSource: 'generated',
      imageAspectRatio: '3:4',
      imageTransform: transform,
      createdAt: '2026-08-04T09:00:00.000Z',
    });

    const imported = parseGameCardForgeImport(JSON.parse(json));

    expect(imported).toEqual({
      faceData,
      imageUrl: imageDataUrl,
      imageSource: 'generated',
      imageAspectRatio: '3:4',
      imageTransform: transform,
    });
  });

  test('exports and imports a card without an illustration', () => {
    const imported = parseGameCardForgeImport(buildGameCardForgeDocument({
      faceData,
      imageDataUrl: null,
      imageSource: null,
      imageAspectRatio: '4:3',
      imageTransform: { scale: 1, x: 0, y: 0 },
    }));

    expect(imported.imageUrl).toBeNull();
    expect(imported.imageSource).toBeNull();
  });

  test('imports the current metadata format and keeps a reachable image URL', () => {
    const imported = parseGameCardForgeImport({
      templateId: '卡牌工坊/游戏卡面',
      faceData,
      imageUrl: 'https://cdn.example.test/card.png',
      imageSource: 'uploaded',
      imageAspectRatio: '16:9',
      imageTransform: transform,
    });

    expect(imported.imageUrl).toBe('https://cdn.example.test/card.png');
    expect(imported.imageAspectRatio).toBe('16:9');
    expect(imported.imageTransform).toEqual(transform);
  });

  test('imports a direct card face JSON without an illustration', () => {
    expect(parseGameCardForgeImport(faceData)).toMatchObject({
      faceData,
      imageUrl: null,
      imageSource: null,
    });
  });

  test('rejects invalid documents without returning partial state', () => {
    expect(() => parseGameCardForgeImport({ faceData: { cardName: '缺字段' } })).toThrow();
    expect(() => parseGameCardForgeImport({
      documentType: 'maho-shojo-game-card-forge',
      documentVersion: 1,
      faceData,
      illustration: {
        dataUrl: 'data:text/plain;base64,SGk=',
        source: 'uploaded',
        aspectRatio: '4:3',
        transform: { scale: 1, x: 0, y: 0 },
      },
    })).toThrow();
  });
});
```

- [ ] **Step 2: 运行测试确认确实失败**

运行：`pnpm test tests/card-forge-document.test.ts`

预期：测试因 `@/lib/card-forge/document` 尚不存在而失败；如果测试通过，说明测试没有锁定新协议，应先检查导入路径或断言。

- [ ] **Step 3: 增加 schema 常量和类型**

在 `lib/schemas/game-card.ts` 的图片 schema 区域增加图片来源和 Data URL schema；在现有 `GameCardFaceDataSchema` 定义之后、`GameCardMetadataSchema` 定义之前增加文档 schema，并让旧 metadata schema 复用 `GameCardImageSourceSchema`：

```ts
export const GAME_CARD_FORGE_DOCUMENT_TYPE = 'maho-shojo-game-card-forge' as const;
export const GAME_CARD_FORGE_DOCUMENT_VERSION = 1 as const;

export const GameCardImageSourceSchema = z.enum(['uploaded', 'data-card', 'generated']);
export type GameCardImageSource = z.infer<typeof GameCardImageSourceSchema>;

const ImageDataUrlSchema = z.string().regex(
  /^data:image\/[a-z0-9.+-]+;base64,[a-z0-9+/=\s]+$/i,
  '插图必须是图片 Base64 Data URL',
);

export const GameCardForgeIllustrationSchema = z.object({
  dataUrl: ImageDataUrlSchema,
  source: GameCardImageSourceSchema,
  aspectRatio: GameCardImageAspectRatioSchema,
  transform: ImageTransformSchema,
});

export const GameCardForgeDocumentSchema = z.object({
  documentType: z.literal(GAME_CARD_FORGE_DOCUMENT_TYPE),
  documentVersion: z.literal(GAME_CARD_FORGE_DOCUMENT_VERSION),
  faceData: GameCardFaceDataSchema,
  illustration: GameCardForgeIllustrationSchema.nullable(),
  createdAt: z.string().datetime().optional(),
}).strict();

export type GameCardForgeIllustration = z.infer<typeof GameCardForgeIllustrationSchema>;
export type GameCardForgeDocument = z.infer<typeof GameCardForgeDocumentSchema>;
```

将 `GameCardMetadataSchema` 中原来的内联图片来源 enum 改为 `GameCardImageSourceSchema`，以避免协议边界出现两套同义枚举。

- [ ] **Step 4: 实现纯 mapper、序列化和兼容解析**

在 `lib/card-forge/document.ts` 实现以下接口：

```ts
import {
  GAME_CARD_FORGE_DOCUMENT_TYPE,
  GAME_CARD_FORGE_DOCUMENT_VERSION,
  GameCardForgeDocumentSchema,
  GameCardMetadataSchema,
  GameCardFaceDataSchema,
  type GameCardForgeDocument,
  type GameCardImageAspectRatio,
  type GameCardImageSource,
  type ImageTransform,
  type GameCardFaceData,
} from '@/lib/schemas/game-card';
import {
  DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO,
  DEFAULT_IMAGE_TRANSFORM,
  isGameCardImageAspectRatio,
  normalizeImageTransform,
} from '@/lib/game-card/image-crop';

export type GameCardForgeRuntimeState = {
  faceData: GameCardFaceData;
  imageUrl: string | null;
  imageSource: GameCardImageSource | null;
  imageAspectRatio: GameCardImageAspectRatio;
  imageTransform: ImageTransform;
};

export type GameCardForgeDocumentInput = {
  faceData: GameCardFaceData;
  imageDataUrl: string | null;
  imageSource: GameCardImageSource | null;
  imageAspectRatio: GameCardImageAspectRatio;
  imageTransform: ImageTransform;
  createdAt?: string;
};

export const buildGameCardForgeDocument = (
  input: GameCardForgeDocumentInput,
): GameCardForgeDocument => GameCardForgeDocumentSchema.parse({
  documentType: GAME_CARD_FORGE_DOCUMENT_TYPE,
  documentVersion: GAME_CARD_FORGE_DOCUMENT_VERSION,
  faceData: input.faceData,
  illustration: input.imageDataUrl
    ? {
        dataUrl: input.imageDataUrl,
        source: input.imageSource ?? 'uploaded',
        aspectRatio: input.imageAspectRatio,
        transform: input.imageTransform,
      }
    : null,
  ...(input.createdAt ? { createdAt: input.createdAt } : {}),
});

export const serializeGameCardForgeDocument = (input: GameCardForgeDocumentInput): string =>
  JSON.stringify(buildGameCardForgeDocument(input), null, 2);

const toRuntimeState = (document: GameCardForgeDocument): GameCardForgeRuntimeState => ({
  faceData: document.faceData,
  imageUrl: document.illustration?.dataUrl ?? null,
  imageSource: document.illustration?.source ?? null,
  imageAspectRatio: document.illustration?.aspectRatio ?? DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO,
  imageTransform: document.illustration?.transform ?? DEFAULT_IMAGE_TRANSFORM,
});

export const parseGameCardForgeImport = (value: unknown): GameCardForgeRuntimeState => {
  const documentResult = GameCardForgeDocumentSchema.safeParse(value);
  if (documentResult.success) return toRuntimeState(documentResult.data);

  const legacyMetadata = GameCardMetadataSchema.safeParse(value);
  if (legacyMetadata.success) {
    return {
      faceData: legacyMetadata.data.faceData,
      imageUrl: legacyMetadata.data.imageUrl,
      imageSource: legacyMetadata.data.imageSource,
      imageAspectRatio: isGameCardImageAspectRatio(legacyMetadata.data.imageAspectRatio)
        ? legacyMetadata.data.imageAspectRatio
        : DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO,
      imageTransform: normalizeImageTransform(legacyMetadata.data.imageTransform),
    };
  }

  const directFace = GameCardFaceDataSchema.safeParse(value);
  if (directFace.success) {
    return {
      faceData: directFace.data,
      imageUrl: null,
      imageSource: null,
      imageAspectRatio: DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO,
      imageTransform: DEFAULT_IMAGE_TRANSFORM,
    };
  }

  throw new Error('导入的 JSON 不是有效的卡牌工坊存档、卡面 JSON 或旧版元数据。');
};
```

不要在 `parseGameCardForgeImport` 中写入 React 状态；它必须在所有校验通过后才返回完整 runtime state。

- [ ] **Step 5: 运行文档测试确认通过**

运行：`pnpm test tests/card-forge-document.test.ts`

预期：5 个测试全部 PASS，且没有 TypeScript/模块解析错误。

- [ ] **Step 6: 提交协议实现**

```bash
git add lib/schemas/game-card.ts lib/card-forge/document.ts tests/card-forge-document.test.ts
git commit -m "feat: 增加卡牌工坊存档协议"
```

## Task 2: 实现插图 Base64 转换边界

**Files:**

- Test: `tests/card-forge-image-data.test.ts`
- Create: `lib/card-forge/image-data.ts`

- [ ] **Step 1: 写失败测试**

在 `tests/card-forge-image-data.test.ts` 中锁定 Data URL、Blob 转换和 fetch 失败行为：

```ts
import { afterEach, describe, expect, test, vi } from 'vitest';
import {
  blobToDataUrl,
  imageUrlToDataUrl,
  isImageDataUrl,
} from '@/lib/card-forge/image-data';

afterEach(() => vi.restoreAllMocks());

describe('card forge image data', () => {
  test('recognizes image Base64 Data URLs only', () => {
    expect(isImageDataUrl('data:image/png;base64,aGVsbG8=')).toBe(true);
    expect(isImageDataUrl('https://cdn.example.test/card.png')).toBe(false);
    expect(isImageDataUrl('data:text/plain;base64,SGk=')).toBe(false);
  });

  test('converts a Blob to a typed Base64 Data URL', async () => {
    const dataUrl = await blobToDataUrl(new Blob(['hello'], { type: 'image/png' }));
    expect(dataUrl).toBe('data:image/png;base64,aGVsbG8=');
  });

  test('keeps an existing image Data URL without fetching', async () => {
    const fetchMock = vi.fn();
    vi.stubGlobal('fetch', fetchMock);
    const dataUrl = 'data:image/webp;base64,aGVsbG8=';
    await expect(imageUrlToDataUrl(dataUrl)).resolves.toBe(dataUrl);
    expect(fetchMock).not.toHaveBeenCalled();
  });

  test('downloads a remote image and converts it to Base64', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => new Response(
      new Blob(['hello'], { type: 'image/jpeg' }),
      { status: 200 },
    )));

    await expect(imageUrlToDataUrl('https://cdn.example.test/card.jpg'))
      .resolves.toBe('data:image/jpeg;base64,aGVsbG8=');
  });

  test('rejects failed responses and non-image responses', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => new Response('not found', { status: 404 })));
    await expect(imageUrlToDataUrl('https://cdn.example.test/missing.png'))
      .rejects.toThrow('图片下载失败');

    vi.stubGlobal('fetch', vi.fn(async () => new Response(
      new Blob(['hello'], { type: 'text/plain' }),
      { status: 200 },
    )));
    await expect(imageUrlToDataUrl('https://cdn.example.test/not-image'))
      .rejects.toThrow('不是有效的图片');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

运行：`pnpm test tests/card-forge-image-data.test.ts`

预期：测试因 `@/lib/card-forge/image-data` 尚不存在而失败。

- [ ] **Step 3: 实现浏览器兼容的转换函数**

在 `lib/card-forge/image-data.ts` 使用 `Blob.arrayBuffer()` 和分块 `btoa`，不引入 Node `Buffer`，保证该模块可被客户端 bundle 和 Cloudflare 浏览器链路使用：

```ts
const IMAGE_DATA_URL_PATTERN = /^data:image\/[a-z0-9.+-]+;base64,[a-z0-9+/=\s]+$/i;

export const isImageDataUrl = (value: unknown): value is string =>
  typeof value === 'string' && IMAGE_DATA_URL_PATTERN.test(value);

export const blobToDataUrl = async (blob: Blob): Promise<string> => {
  const bytes = new Uint8Array(await blob.arrayBuffer());
  const chunks: string[] = [];
  for (let index = 0; index < bytes.length; index += 0x8000) {
    chunks.push(String.fromCharCode(...bytes.subarray(index, index + 0x8000)));
  }
  const base64 = btoa(chunks.join(''));
  const mimeType = blob.type || 'application/octet-stream';
  return `data:${mimeType};base64,${base64}`;
};

export const imageUrlToDataUrl = async (imageUrl: string): Promise<string> => {
  if (isImageDataUrl(imageUrl)) return imageUrl;

  let response: Response;
  try {
    response = await fetch(imageUrl);
  } catch (error) {
    throw new Error('图片下载失败，无法将插图嵌入存档。', { cause: error });
  }
  if (!response.ok) {
    throw new Error(`图片下载失败（HTTP ${response.status}），无法将插图嵌入存档。`);
  }

  const blob = await response.blob();
  if (!blob.type.toLowerCase().startsWith('image/')) {
    throw new Error('下载内容不是有效的图片，无法将插图嵌入存档。');
  }
  return blobToDataUrl(blob);
};
```

- [ ] **Step 4: 运行图片转换测试确认通过**

运行：`pnpm test tests/card-forge-image-data.test.ts`

预期：5 个测试全部 PASS。

- [ ] **Step 5: 提交图片转换边界**

```bash
git add lib/card-forge/image-data.ts tests/card-forge-image-data.test.ts
git commit -m "feat: 支持卡牌工坊插图内嵌导出"
```

## Task 3: 重构 CardForgePage 的导入导出状态流程

**Files:**

- Test: `tests/generate-game-card-handler.test.tsx`
- Modify: `components/card-forge/CardForgePage.tsx`

- [ ] **Step 1: 先增加未生成状态的页面失败断言**

在现有 `tests/generate-game-card-handler.test.tsx` 增加：

```tsx
test('卡牌尚未生成时仍展示卡面存档导入入口并禁用导出', () => {
  const html = renderToStaticMarkup(<CardForgePage />);

  expect(html).toContain('卡面存档');
  expect(html).toContain('导入卡面 JSON');
  expect(html).toMatch(/<button[^>]*disabled[^>]*>导出卡面 JSON/);
});
```

运行：`pnpm test tests/generate-game-card-handler.test.tsx`

预期：测试因现有按钮名和条件渲染不满足而失败，证明 UI 回归测试捕获了用户报告的问题。

- [ ] **Step 2: 增加页面所需的状态和导入导出依赖**

在 `CardForgePage.tsx` 增加导入：

```ts
import {
  parseGameCardForgeImport,
  serializeGameCardForgeDocument,
} from '@/lib/card-forge/document';
import { imageUrlToDataUrl } from '@/lib/card-forge/image-data';
```

在已有状态附近增加：

```ts
const [isExportingJson, setIsExportingJson] = useState(false);
const [cardForgeFileError, setCardForgeFileError] = useState<string | null>(null);
```

保留 `imageUrl` 作为运行时图片地址；导出前才把它转换成 `imageDataUrl`，避免 Base64 在编辑期间重复占用内存。

- [ ] **Step 3: 用异步导出处理器替换旧元数据导出**

将旧 `handleExportJson` 替换为：

```ts
const handleExportJson = useCallback(async () => {
  if (!effectiveFaceData || isExportingJson) return;
  setIsExportingJson(true);
  setCardForgeFileError(null);

  try {
    const imageDataUrl = imageUrl ? await imageUrlToDataUrl(imageUrl) : null;
    const json = serializeGameCardForgeDocument({
      faceData: effectiveFaceData,
      imageDataUrl,
      imageSource,
      imageAspectRatio,
      imageTransform,
      createdAt: new Date().toISOString(),
    });
    const sanitized = effectiveFaceData.cardName.replace(/[^a-z0-9\u4e00-\u9fa5]/gi, '_');
    downloadBlob(
      new Blob([json], { type: 'application/json' }),
      `卡牌_${sanitized}.json`,
    );
  } catch (error) {
    setCardForgeFileError(error instanceof Error ? error.message : '卡面存档导出失败');
  } finally {
    setIsExportingJson(false);
  }
}, [effectiveFaceData, imageAspectRatio, imageSource, imageTransform, imageUrl, isExportingJson]);
```

导出失败时只更新 `cardForgeFileError`，不改变已有卡面和生成状态。

- [ ] **Step 4: 用统一 parser 替换旧导入处理器**

将旧 `handleImportJson` 替换为基于 `File.text()` 的异步处理器，并在开始时重置 input：

```ts
const handleImportJson = async (event: React.ChangeEvent<HTMLInputElement>) => {
  const input = event.currentTarget;
  const file = input.files?.[0];
  input.value = '';
  if (!file) return;

  try {
    const imported = parseGameCardForgeImport(JSON.parse(await file.text()));
    setFaceData(imported.faceData);
    setImageUrl(imported.imageUrl);
    setImageSource(imported.imageSource);
    setImageAspectRatio(imported.imageAspectRatio);
    setImageTransform(imported.imageTransform);
    setThemeColorOverride(null);
    setSourceCardKind(null);
    setCardForgeFileError(null);
    setGenError(null);
    setGenErrorStatus(null);
    setGenStatus('success');
  } catch (error) {
    setCardForgeFileError(error instanceof Error ? error.message : '卡面存档导入失败');
  }
};
```

该函数只有在 parser 成功返回完整 runtime state 后才更新状态，确保坏文件不会清空现有卡面。

- [ ] **Step 5: 运行类型检查和页面测试确认仍然失败/通过状态正确**

运行：`pnpm exec tsc --noEmit` 和 `pnpm test tests/generate-game-card-handler.test.tsx`

预期：类型检查 PASS；页面新增测试仍可能因 JSX 尚未重排而失败，下一步完成 UI 后应转为 PASS。

## Task 4: 调整卡面存档 UI，保证空状态可导入

**Files:**

- Modify: `components/card-forge/CardForgePage.tsx`

- [ ] **Step 1: 将主题色与存档区域拆分为条件与常驻两部分**

保留主题色选择仅在 `effectiveFaceData` 存在时渲染；把存档区域移到条件外，使用以下结构替换旧的 `{effectiveFaceData && (...)}` 整段导入/导出区：

```tsx
{effectiveFaceData && (
  <section className="card-forge-panel rounded-2xl p-5 space-y-4">
    <h2 className="text-lg font-semibold text-[var(--app-text)]">主题色</h2>
    <div className="flex flex-wrap gap-2">
      {PRESET_COLORS.map((color) => (
        <button
          key={color}
          type="button"
          onClick={() => setThemeColorOverride(color)}
          className="w-8 h-8 rounded-full border-2 transition-transform hover:scale-110"
          style={{
            backgroundColor: color,
            borderColor: (themeColorOverride ?? effectiveFaceData.themeColor) === color ? '#fff' : 'transparent',
            boxShadow: (themeColorOverride ?? effectiveFaceData.themeColor) === color
              ? `0 0 0 2px ${color}`
              : 'none',
          }}
        />
      ))}
      <label className="w-8 h-8 rounded-full border-2 border-gray-300 dark:border-gray-600 cursor-pointer flex items-center justify-center overflow-hidden relative">
        <input
          type="color"
          value={themeColorOverride ?? effectiveFaceData.themeColor}
          onChange={(event) => setThemeColorOverride(event.target.value)}
          className="opacity-0 absolute w-8 h-8"
          aria-label="自定义主题色"
        />
        <span className="text-xs">+</span>
      </label>
    </div>
  </section>
)}

<section className="card-forge-panel rounded-2xl p-5 space-y-4">
  <div>
    <h2 className="text-lg font-semibold text-[var(--app-text)]">卡面存档</h2>
    <p className="mt-1 text-xs text-[var(--app-text-muted)]">
      导出会将插图内嵌到 JSON，文件较大但可以离线恢复卡面。
    </p>
  </div>
  <div className="flex gap-2">
    <button
      type="button"
      onClick={() => void handleExportJson()}
      disabled={!effectiveFaceData || isExportingJson}
      className="flex-1 px-3 py-2 bg-green-50 text-green-700 rounded-xl text-sm font-medium disabled:cursor-not-allowed disabled:opacity-50 dark:bg-green-950/30 dark:text-green-400"
    >
      {isExportingJson ? '正在嵌入插图…' : '导出卡面 JSON'}
    </button>
    <label className="flex-1 px-3 py-2 bg-blue-50 text-blue-700 rounded-xl text-sm font-medium hover:bg-blue-100 transition-colors cursor-pointer text-center dark:bg-blue-950/30 dark:text-blue-400">
      导入卡面 JSON
      <input
        type="file"
        accept=".json,application/json"
        className="hidden"
        onChange={(event) => void handleImportJson(event)}
      />
    </label>
  </div>
  {cardForgeFileError && (
    <div className="rounded-lg border border-red-300 bg-red-50 px-3 py-2 text-xs text-red-700 dark:border-red-800 dark:bg-red-950/40 dark:text-red-300">
      {cardForgeFileError}
    </div>
  )}
</section>
```

按钮必须明确设置 `type="button"`，避免位于表单上下文时触发提交；文件输入保留 `.json,application/json` 限制。

- [ ] **Step 2: 运行页面回归测试**

运行：`pnpm test tests/generate-game-card-handler.test.tsx`

预期：接口超过 50,000 字符测试、Token 指示器测试和新增空状态存档 UI 测试全部 PASS。

- [ ] **Step 3: 运行相关功能全量测试**

运行：`pnpm test tests/card-forge-document.test.ts tests/card-forge-image-data.test.ts tests/game-card-image-crop.test.ts tests/generate-game-card-handler.test.tsx`

预期：4 个测试文件全部 PASS；若旧测试失败，只修复本次导入/导出重构造成的契约变化，不改变既有裁剪数学断言。

- [ ] **Step 4: 提交页面重构**

```bash
git add components/card-forge/CardForgePage.tsx tests/generate-game-card-handler.test.tsx
git commit -m "feat: 重构卡牌工坊卡面存档导入导出"
```

## Task 5: 完成全量验证和交付检查

**Files:**

- Verify: `lib/schemas/game-card.ts`
- Verify: `lib/card-forge/document.ts`
- Verify: `lib/card-forge/image-data.ts`
- Verify: `components/card-forge/CardForgePage.tsx`
- Verify: `tests/card-forge-document.test.ts`
- Verify: `tests/card-forge-image-data.test.ts`
- Verify: `tests/generate-game-card-handler.test.tsx`

- [ ] **Step 1: 运行完整测试套件**

运行：`pnpm test`

预期：Vitest 退出码为 0，失败数为 0。

- [ ] **Step 2: 运行 TypeScript 检查**

运行：`pnpm exec tsc --noEmit`

预期：退出码为 0，没有新增类型错误。

- [ ] **Step 3: 运行 lint**

运行：`pnpm lint`

预期：退出码为 0，没有新增 ESLint 错误。

- [ ] **Step 4: 运行生产构建**

运行：`pnpm build`

预期：Next.js 生产构建退出码为 0，客户端 bundle 能解析 `lib/card-forge/image-data.ts`，不出现 Node `Buffer` 或服务端模块错误。

- [ ] **Step 5: 检查差异和需求覆盖**

运行：

```bash
git diff --check HEAD~4..HEAD
git status --short
```

逐项确认：

- 空白卡牌页面始终有“导入卡面 JSON”。
- 有卡面时可以导出新版存档 JSON。
- 导出图片是 Base64 Data URL，而不是远程 URL。
- 导入能恢复卡面、图片、比例和裁剪变换。
- 非法导入不会清空已有状态。
- 不保存令牌和 provider 配置。

- [ ] **Step 6: 提交验证记录**

```bash
git add docs/plans/2026-08-04_094157_卡牌工坊存档JSON实现计划.md
git commit -m "docs: 补充卡牌工坊存档实现计划"
```

## 计划自检

- 设计文档中的导入入口常驻、Base64 内嵌、裁剪状态恢复、旧格式兼容、非法输入不覆盖状态，分别由 Task 3、Task 1、Task 4、Task 1、Task 3 覆盖。
- 图片下载失败路径由 Task 2 的测试和 Task 3 的错误状态覆盖。
- 未新增数据库、API、ZIP 或凭据字段，符合非目标范围。
- 所有函数名、字段名和类型在后续任务中保持一致：`buildGameCardForgeDocument`、`serializeGameCardForgeDocument`、`parseGameCardForgeImport`、`imageUrlToDataUrl`。
- 计划中没有 `TBD`、`TODO` 或未定义的“适当处理”步骤；每个代码步骤给出目标文件、接口和验证命令。
