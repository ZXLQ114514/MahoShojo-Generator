# 卡牌工坊插图裁剪与数据卡入口实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为卡牌工坊加入可选插图比例、可拖拽/缩放的裁剪工作台，并复用线上数据卡浏览模态框完成数据卡选择输入。

**Architecture:** 将图片比例、cover 布局和位移边界收敛到可测试的 `lib/game-card/image-crop.ts`；新增受控 `ImageCropEditor` 处理指针交互；`CardForgePage` 只协调输入、裁剪状态与 `BattleDataModal`。`GameCardFace` 和卡牌元数据 schema 共享同一套比例/变换类型，保证预览、导出和重新导入一致。

**Tech Stack:** Next.js App Router、React 19、TypeScript strict、Tailwind 4、现有 `BattleDataModal`、Vitest、浏览器 Pointer Events/ResizeObserver。

---

## 文件结构与职责

- Create: `lib/game-card/image-crop.ts` — 图片比例、变换类型、cover 布局、位移边界和元数据归一化的纯函数。
- Create: `lib/card-forge/source-card.ts` — 将线上数据卡选择 payload 去除传输元数据并格式化为输入 JSON。
- Create: `components/card-forge/ImageCropEditor.tsx` — 受控裁剪视窗、拖拽、滚轮/捏合缩放、比例选择和精细调整控件。
- Modify: `lib/schemas/game-card.ts` — 增加卡面插图比例/变换 schema，并扩展 `GameCardMetadataSchema`。
- Modify: `components/game-card/GameCardFace.tsx` — 使用共享图片布局状态和可选插图比例，保留 4:3 默认行为。
- Modify: `components/card-forge/CardForgePage.tsx` — 接入裁剪工作台、本地/线上数据卡入口和元数据导入导出字段。
- Modify: `styles/globals.css` — 增加裁剪视窗、网格和拖拽状态样式。
- Create: `tests/game-card-image-crop.test.ts` — 覆盖比例、cover 布局、边界和旧值归一化。
- Create: `tests/card-forge-source-card.test.ts` — 覆盖线上数据卡 payload 清理与 JSON 格式化。

## Task 1: 先锁定图片裁剪纯逻辑

**Files:**

- Create: `tests/game-card-image-crop.test.ts`
- Create: `lib/game-card/image-crop.ts`
- Modify: `lib/schemas/game-card.ts:3-71`

- [x] **Step 1: 写失败测试**

在 `tests/game-card-image-crop.test.ts` 中先导入尚不存在的纯函数，并覆盖以下行为：

```ts
import { describe, expect, test } from 'vitest';
import {
  DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO,
  clampImageTransform,
  getCoverLayout,
  normalizeImageTransform,
} from '@/lib/game-card/image-crop';

describe('game card image crop math', () => {
  test('portrait image uses cover layout and exposes vertical overflow', () => {
    const layout = getCoverLayout(
      { width: 800, height: 1200 },
      { width: 400, height: 300 },
      1,
    );

    expect(layout.width).toBe(400);
    expect(layout.height).toBe(600);
    expect(layout.maxPanX).toBe(0);
    expect(layout.maxPanY).toBe(150);
  });

  test('clampImageTransform prevents pan beyond the scaled image boundary', () => {
    const layout = getCoverLayout({ width: 1200, height: 800 }, { width: 400, height: 300 }, 2);
    const clamped = clampImageTransform({ scale: 2, x: 1.8, y: -1.8 }, layout);

    expect(clamped.x).toBe(1);
    expect(clamped.y).toBe(-1);
  });

  test('invalid metadata falls back to defaults', () => {
    expect(normalizeImageTransform({ scale: 99, x: 'bad', y: null })).toEqual({ scale: 1, x: 0, y: 0 });
    expect(DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO).toBe('4:3');
  });
});
```

- [x] **Step 2: 运行测试确认确实失败**

运行：`pnpm test tests/game-card-image-crop.test.ts`

预期：测试因 `@/lib/game-card/image-crop` 不存在而失败；如果测试先通过，说明测试没有锁定新行为，应先修正测试。

- [x] **Step 3: 增加 schema 类型和纯函数实现**

在 `lib/schemas/game-card.ts` 增加 canonical 比例和变换协议：

```ts
export const GAME_CARD_IMAGE_ASPECT_RATIOS = ['4:3', '1:1', '3:4', '16:9'] as const;
export const GameCardImageAspectRatioSchema = z.enum(GAME_CARD_IMAGE_ASPECT_RATIOS);
export type GameCardImageAspectRatio = z.infer<typeof GameCardImageAspectRatioSchema>;

export const ImageTransformSchema = z.object({
  scale: z.number().min(1).max(3),
  x: z.number().min(-1).max(1),
  y: z.number().min(-1).max(1),
});
export type ImageTransform = z.infer<typeof ImageTransformSchema>;
```

将 `GameCardMetadataSchema` 扩展为：

```ts
imageAspectRatio: GameCardImageAspectRatioSchema.optional(),
imageTransform: ImageTransformSchema.optional(),
```

在 `lib/game-card/image-crop.ts` 提供：

```ts
export type ImageSize = { width: number; height: number };
export type CropViewport = { width: number; height: number };
export type CoverLayout = {
  width: number;
  height: number;
  maxPanX: number;
  maxPanY: number;
};

export const DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO: GameCardImageAspectRatio = '4:3';
export const DEFAULT_IMAGE_TRANSFORM: ImageTransform = { scale: 1, x: 0, y: 0 };

export const getAspectRatioValue = (value: GameCardImageAspectRatio): number => {
  const [width, height] = value.split(':').map(Number);
  return width / height;
};

export const getCoverLayout = (
  image: ImageSize,
  viewport: CropViewport,
  scale: number,
): CoverLayout => {
  const baseScale = Math.max(viewport.width / image.width, viewport.height / image.height);
  const width = image.width * baseScale * scale;
  const height = image.height * baseScale * scale;
  return {
    width,
    height,
    maxPanX: Math.max(0, (width - viewport.width) / 2),
    maxPanY: Math.max(0, (height - viewport.height) / 2),
  };
};

export const clampImageTransform = (transform: ImageTransform, layout: CoverLayout): ImageTransform => ({
  scale: Math.min(3, Math.max(1, transform.scale)),
  x: layout.maxPanX > 0 ? Math.min(1, Math.max(-1, transform.x)) : 0,
  y: layout.maxPanY > 0 ? Math.min(1, Math.max(-1, transform.y)) : 0,
});
```

`getCoverLayout` 的 `maxPanX/maxPanY` 以图片中心为原点；渲染层将归一化的 `x/y` 乘以对应最大位移。`normalizeImageTransform` 对导入 JSON 做有限数字、范围和默认值校验，禁止旧/外部 JSON 让组件进入非法状态。

- [x] **Step 4: 运行测试确认通过**

运行：`pnpm test tests/game-card-image-crop.test.ts`

预期：新增测试全部通过。

- [x] **Step 5: 提交纯逻辑变更**

```bash
git add lib/schemas/game-card.ts lib/game-card/image-crop.ts tests/game-card-image-crop.test.ts
git commit -m "feat: 增加卡面插图裁剪模型"
```

## Task 2: 锁定线上数据卡输入转换

**Files:**

- Create: `lib/card-forge/source-card.ts`
- Create: `tests/card-forge-source-card.test.ts`

- [x] **Step 1: 写失败测试**

```ts
import { describe, expect, test } from 'vitest';
import { formatSelectedDataCardJson } from '@/lib/card-forge/source-card';

describe('formatSelectedDataCardJson', () => {
  test('removes battle transport metadata while preserving card content', () => {
    const result = formatSelectedDataCardJson({
      templateId: '魔法少女/角色',
      codename: '蔷薇荆棘',
      _cardId: 'card-1',
      _cardName: '蔷薇荆棘',
      _cardType: 'character',
      _author: '作者',
      nested: { _updatedAt: 'ignored', value: 1 },
    });

    expect(JSON.parse(result)).toEqual({
      templateId: '魔法少女/角色',
      codename: '蔷薇荆棘',
      nested: { value: 1 },
    });
  });
});
```

- [x] **Step 2: 运行测试确认失败**

运行：`pnpm test tests/card-forge-source-card.test.ts`

预期：因 `formatSelectedDataCardJson` 尚不存在而失败。

- [x] **Step 3: 实现边界适配器**

在 `lib/card-forge/source-card.ts` 只做现有 mapper 的组合，不在页面中散落兼容字段：

```ts
import { stripBattleSelectionTransportMeta } from '@/lib/data-card-read-mappers';

export const formatSelectedDataCardJson = (payload: unknown): string =>
  JSON.stringify(stripBattleSelectionTransportMeta(payload), null, 2);
```

- [x] **Step 4: 运行测试确认通过**

运行：`pnpm test tests/card-forge-source-card.test.ts`

预期：测试通过，且没有改动数据卡 mapper 的既有行为。

- [x] **Step 5: 提交输入适配器**

```bash
git add lib/card-forge/source-card.ts tests/card-forge-source-card.test.ts
git commit -m "feat: 适配卡牌工坊数据卡输入"
```

## Task 3: 实现受控图片裁剪编辑器

**Files:**

- Create: `components/card-forge/ImageCropEditor.tsx`
- Modify: `styles/globals.css:卡牌工坊页面样式区域`

- [x] **Step 1: 创建受控组件骨架并保留失败类型检查**

组件 props 固定为：

```ts
interface ImageCropEditorProps {
  imageUrl: string;
  imageAspectRatio: GameCardImageAspectRatio;
  imageTransform: ImageTransform;
  onAspectRatioChange: (value: GameCardImageAspectRatio) => void;
  onTransformChange: (value: ImageTransform) => void;
  onReset: () => void;
}
```

组件使用 `ResizeObserver` 获取视窗尺寸，`HTMLImageElement.naturalWidth/naturalHeight` 获取图片尺寸，通过 `getCoverLayout` 计算实际绘制宽高和最大位移。图片以绝对定位居中渲染：

```tsx
<img
  src={imageUrl}
  alt="插图裁剪预览"
  draggable={false}
  style={{
    width: layout.width,
    height: layout.height,
    left: '50%',
    top: '50%',
    transform: `translate(-50%, -50%) translate(${imageTransform.x * layout.maxPanX}px, ${imageTransform.y * layout.maxPanY}px)`,
  }}
/>
```

视窗叠加三分网格，且设置 `touch-action: none`。指针按下时保存 pointer 起点并调用 `setPointerCapture`；移动时将像素增量换算成归一化 `x/y`，再用 `clampImageTransform` 限制范围。

- [x] **Step 2: 增加缩放、捏合和键盘可达控件**

缩放滑块范围固定为 `1` 到 `3`、步进 `0.05`；滚轮按 `0.1` 调整。两个 pointer 同时存在时保存初始距离，按距离变化调整 scale，并在每次变化后重新限制 transform。精细调整使用两个 range：

```tsx
<input aria-label="图片水平位置" type="range" min={-1} max={1} step={0.01} ... />
<input aria-label="图片垂直位置" type="range" min={-1} max={1} step={0.01} ... />
```

比例 `<select>` 的选项来自 `GAME_CARD_IMAGE_ASPECT_RATIOS`，重置按钮带 `type="button"` 和 `aria-label="重置图片裁剪"`。

- [x] **Step 3: 增加裁剪视窗样式与空/加载/错误状态**

在 `styles/globals.css` 增加 `.card-forge-crop-editor`、`.card-forge-crop-viewport`、`.card-forge-crop-grid` 和 `.card-forge-crop-viewport.is-dragging`。视窗使用 `aspect-ratio: var(--card-forge-crop-ratio)`、`overflow: hidden`、圆角和高对比度边框；移动端宽度为 100%。图片加载失败时渲染行内错误文本，不使用 `alert`。

- [x] **Step 4: 运行类型检查**

运行：`pnpm exec tsc --noEmit`

预期：组件 props、schema 类型和纯函数调用无 TypeScript 错误。

- [x] **Step 5: 提交裁剪编辑器**

```bash
git add components/card-forge/ImageCropEditor.tsx styles/globals.css
git commit -m "feat: 增加卡牌工坊插图裁剪编辑器"
```

## Task 4: 让卡面预览复用比例和布局

**Files:**

- Modify: `components/game-card/GameCardFace.tsx:8-38,65-216`
- Modify: `styles/globals.css:1828-1852`

- [x] **Step 1: 更新 `GameCardFace` props 和默认值**

从 `lib/game-card/image-crop.ts`/`lib/schemas/game-card.ts` 导入共享 `ImageTransform`、默认变换和比例类型；保留兼容导出，避免当前页面已有 import 失效：

```ts
export type { ImageTransform } from '@/lib/schemas/game-card';
export { DEFAULT_IMAGE_TRANSFORM } from '@/lib/game-card/image-crop';
```

在 props 增加：

```ts
imageAspectRatio?: GameCardImageAspectRatio;
```

插图 frame 使用 `style={{ aspectRatio: getAspectRatioValue(imageAspectRatio) }}`，缺省为 `4 / 3`；`getAspectRatioValue` 返回 CSS `aspect-ratio` 可接受的数值。

- [x] **Step 2: 使用同一 cover layout 渲染预览图**

在 `GameCardFace` 内监听卡面插图容器尺寸与图片自然尺寸，使用 `getCoverLayout` 计算宽高和最大位移；加载前保留现有 `object-fit: cover` 作为降级，加载后使用与编辑器相同的绝对定位公式。这样编辑器和右侧卡面预览共享同一归一化 `x/y` 语义。

- [x] **Step 3: 运行已有卡面测试与类型检查**

运行：`pnpm test tests/output-card-stop-button.test.tsx` 以及 `pnpm exec tsc --noEmit`。

预期：旧卡面渲染、保存按钮和没有 `imageAspectRatio` 的调用方保持通过。

- [x] **Step 4: 提交卡面兼容改动**

```bash
git add components/game-card/GameCardFace.tsx styles/globals.css
git commit -m "feat: 让卡面预览支持插图比例"
```

## Task 5: 接入 CardForgePage 的裁剪和线上数据卡入口

**Files:**

- Modify: `components/card-forge/CardForgePage.tsx:8-18,150-458,484-786,843-915`

- [x] **Step 1: 增加页面状态和线上选择回调**

增加：

```ts
const [imageAspectRatio, setImageAspectRatio] = useState<GameCardImageAspectRatio>(DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO);
const [isDataCardModalOpen, setIsDataCardModalOpen] = useState(false);
```

线上选择回调使用适配器，并清理旧的生成错误：

```ts
const handleSelectOnlineDataCard = useCallback((payload: unknown) => {
  setSourceCardJson(formatSelectedDataCardJson(payload));
  setGenError(null);
  setGenErrorStatus(null);
  setGenStatus('idle');
  setIsDataCardModalOpen(false);
}, []);
```

图片上传、立绘生成和移除图片都同时调用 `setImageTransform(DEFAULT_IMAGE_TRANSFORM)` 与 `setImageAspectRatio(DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO)`。

- [x] **Step 2: 重做数据卡输入操作区**

在现有“导入 JSON / 示例”位置加入 `浏览线上卡` 按钮，文本框继续作为粘贴入口，placeholder 明确写出“可直接粘贴 JSON”。页面底部渲染：

```tsx
<BattleDataModal
  isOpen={isDataCardModalOpen}
  onClose={() => setIsDataCardModalOpen(false)}
  onSelectCard={handleSelectOnlineDataCard}
  selectedType="all"
  allowedTypes={['character', 'scenario', 'history', 'questionnaire']}
  visibleTabs={['my', 'public', 'favorites']}
  initialTab="public"
  selectionMode="single"
  titleOverride="选择数据卡"
/>
```

导入本地 JSON 时保留原文本内容，不强行重写字段；读取失败继续显示现有生成错误横幅。

- [x] **Step 3: 用 `ImageCropEditor` 替换小缩略图和滑块面板**

有图片时渲染：

```tsx
<ImageCropEditor
  imageUrl={imageUrl}
  imageAspectRatio={imageAspectRatio}
  imageTransform={imageTransform}
  onAspectRatioChange={setImageAspectRatio}
  onTransformChange={setImageTransform}
  onReset={() => setImageTransform(DEFAULT_IMAGE_TRANSFORM)}
/>
```

保留“移除图片”操作，并把右侧 `GameCardFace` 传入 `imageAspectRatio={imageAspectRatio}`。

- [x] **Step 4: 扩展元数据导出和导入**

导出对象增加：

```ts
imageAspectRatio,
imageTransform,
```

导入时使用 `normalizeImageTransform(parsed.imageTransform)`，比例只接受 `GAME_CARD_IMAGE_ASPECT_RATIOS` 中的值，否则使用 `DEFAULT_GAME_CARD_IMAGE_ASPECT_RATIO`。导入旧版元数据仍按现有 `faceData/imageUrl/imageSource` 逻辑工作。

- [x] **Step 5: 运行针对性测试和类型检查**

运行：`pnpm test tests/game-card-image-crop.test.ts tests/card-forge-source-card.test.ts` 与 `pnpm exec tsc --noEmit`。

预期：纯逻辑测试通过，页面编译无错误。

- [x] **Step 6: 提交页面整合**

```bash
git add components/card-forge/CardForgePage.tsx
git commit -m "feat: 完善卡牌工坊数据卡与裁剪入口"
```

## Task 6: 对照规格做静态复核和完整验证

**Files:**

- Inspect: `docs/specs/2026-08-04_卡牌工坊插图裁剪与数据卡入口设计.md`
- Inspect: `components/card-forge/CardForgePage.tsx`
- Inspect: `components/card-forge/ImageCropEditor.tsx`
- Inspect: `components/game-card/GameCardFace.tsx`

- [x] **Step 1: 运行完整测试**

运行：`pnpm test`

预期：Vitest 退出码为 0，新增测试和既有测试均通过。

- [x] **Step 2: 运行 lint**

运行：`pnpm lint`

预期：Next lint 退出码为 0；若仓库当前 Next 版本只输出迁移提示，记录提示但不引入新的 lint error。

- [x] **Step 3: 运行生产构建**

运行：`pnpm build`

预期：Next production build 退出码为 0。

- [x] **Step 4: 检查改动范围和命名**

运行：`git diff --check`、`pnpm check:naming`、`git status --short`。

确认没有新增第三方依赖、没有修改生成 API 和数据卡权限逻辑，新增 TypeScript 业务字段保持 camelCase，文档和测试路径符合仓库约定。

- [ ] **Step 5: 手动验收页面交互（待在浏览器中确认）**

> 当前未启动浏览器执行该项手工验收；已完成自动化测试、lint、生产构建和静态变更复核。

启动 `pnpm dev`，在 `/card-forge` 验收：

1. 上传横图、竖图、方图，切换四种比例，拖拽和缩放后观察右侧卡面同步；
2. 用键盘操作比例、缩放和精细调整；
3. 保存 PNG 后确认构图与预览一致；
4. 粘贴 JSON、导入本地 JSON、打开线上模态框选择公开/我的/收藏卡；
5. 退出登录后确认线上入口仍能选择公开卡；
6. 导出再导入元数据，确认比例和构图恢复。

- [x] **Step 6: 最终复核并提交必要的剩余改动**

```bash
git diff --stat
git status --short
```

若前述任务均已分别提交，则只确认工作树状态；若存在尚未提交的格式或测试修正，使用中文 Conventional Commit 标题单独提交，并在交付说明中记录所有验证命令结果。
