import { createAdminImageGenerationLicensesHandler } from './handler';

const handler = createAdminImageGenerationLicensesHandler();

export const GET = handler;
export const POST = handler;
export const DELETE = handler;
